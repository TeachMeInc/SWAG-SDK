# ToolbarState (Interface)

The `ToolbarState` interface represents the state of the SDK's toolbar, including its items.

## Fields

| Field   | Type         | Description                                 |
|---------|--------------|---------------------------------------------|
| `items` | ToolbarItem[]| Array of toolbar items.                     |

## Usage

Used to manage and update the state of the SDK's toolbar.